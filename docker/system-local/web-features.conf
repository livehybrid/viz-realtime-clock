# The Studio custom-viz "extension framework" is behind a feature flag, off by
# default. Without it the viz type resolves but the sandbox never mounts the
# module (spinner forever, no errors). Discovered by diffing a working
# instance's /services/web-features (2026-08-18).
[feature:dashboard_studio]
activate_studio_extension_framework = true
